# URL of the FastAPI backend (main.py). The Next.js API routes under
# app/api/*/route.ts proxy to this server-side, so the browser never talks
# to it directly and no CORS setup is required.
BACKEND_URL=http://localhost:8000
